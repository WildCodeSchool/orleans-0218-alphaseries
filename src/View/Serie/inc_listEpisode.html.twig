{% for episode in episodes %}
    <li class="list-group-item bgc">
        <label class="switch">
            <input type="checkbox" id="switch">
            <span class="slider round"></span>
        </label >
        E{{ episode.number }}  - {{ episode.title }}
    </li>
{% endfor %}